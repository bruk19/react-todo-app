import React from 'react';

const Header = () => {
    const headers = {
        padding: '15px'
    };
    return (
        <Header style={headers}>
          <h1>
            style={{
                fontsize: '15px',
            }}
          </h1>
        </Header>
);
};

export default Header;
